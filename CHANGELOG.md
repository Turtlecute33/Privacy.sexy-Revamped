# Changelog

This fork does not publish tagged releases. Every commit merged to `master` is built and deployed
straight to <https://privacy.turtlecute.org>, so `git log` is the authoritative history of changes.

Release history up to and including `0.13.8`, the point this project forked, belongs to upstream and is
kept there: <https://github.com/undergroundwires/privacy.sexy/blob/master/CHANGELOG.md>.
